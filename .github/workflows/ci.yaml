name: vault

on:
  workflow_dispatch:

  push:
    branches:
      - main

jobs:
  vault-secret:
    runs-on: ubuntu-latest

    steps:   
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Vault
        uses: hashicorp/vault-action@v2
        with:
          url: https://vault.dev.jackhenryorigin.com:8200
          caCertificate: ${{ vars.DEV_OGN_SVCS_VAULT_CA_CERT }}
          method: github
          githubToken: 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
